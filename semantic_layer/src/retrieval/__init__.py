"""
Retrieval package — the core intelligence of the semantic layer.

Orchestrates: intent extraction → ANN search → graph traversal →
explore scoring → ambiguity detection → context assembly.

This is where natural language questions become structured field selections.
"""
